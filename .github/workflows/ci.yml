name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install ShellCheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Run smoke tests
        run: tests/smoke.sh
      - name: Verify repository contains no bundled Windows/LINE binaries
        run: tests/no-bundled-binaries.sh
